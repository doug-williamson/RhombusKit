/**
 * GoatCounter endpoint for showcase analytics — privacy-friendly, cookieless,
 * and consent-banner-free.
 *
 * Leave this empty to disable analytics entirely (the default until the
 * production site is live). When empty, {@link AnalyticsService} is a complete
 * no-op: no script is loaded and no events are sent.
 *
 * At launch: create a free site at https://www.goatcounter.com/ and set this to
 * its count endpoint, e.g. `https://rhombuskit.goatcounter.com/count`.
 */
export const GOATCOUNTER_ENDPOINT = '';
