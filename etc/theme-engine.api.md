# API Report — @rhombuskit/theme-engine

> Generated by `tools/api-snapshot.mjs` — do not edit by hand.
> The frozen public export surface. Drift is reviewed under the semver
> policy; regenerate intentionally with `node tools/api-snapshot.mjs --update`.

### RHOMBUS_THEME_CONFIG (const)
  RHOMBUS_THEME_CONFIG: _angular_core.InjectionToken<RhombusThemeConfig>

### RHOMBUS_THEME_DEFAULT_CONFIG (const)
  RHOMBUS_THEME_DEFAULT_CONFIG: RhombusThemeConfig

### RhombusThemeConfig (interface)
  dark: keyof ThemeRegistry
  default: ThemePreference
  light: keyof ThemeRegistry

### RhombusThemeService (class)
  readonly current: _angular_core.Signal<keyof ThemeRegistry>
  readonly preference: _angular_core.Signal<ThemePreference>
  setTheme: (preference: ThemePreference) => void
  toggle: () => void

### THEME_ATTRIBUTE (const)
  THEME_ATTRIBUTE: "data-theme"

### THEME_INIT_SCRIPT (const)
  THEME_INIT_SCRIPT: string

### THEME_STORAGE_KEY (const)
  THEME_STORAGE_KEY: "rhombuskit:theme-preference"

### ThemeName (type)
  type ThemeName = keyof ThemeRegistry

### ThemePreference (type)
  type ThemePreference = ThemeName | 'system'

### ThemeRegistry (interface)
  rhombus-dark: true
  rhombus-light: true

### getThemeInitScript (function)
  getThemeInitScript: (config?: RhombusThemeConfig) => string

### provideRhombusTheme (function)
  provideRhombusTheme: (config?: Partial<RhombusThemeConfig>) => EnvironmentProviders
