/** Public type surface for the RhombusKit carousel. */

/**
 * How a {@link RhombusCarouselComponent} transitions between slides:
 * `'slide'` translates the track horizontally; `'fade'` cross-fades in place.
 */
export type RhombusCarouselTransition = 'slide' | 'fade';
