// Re-exports CONTRACT and SemanticTokenName from types.ts.
// Exists as a spec file so the generator has a single import target for the contract.
export { CONTRACT, type SemanticTokenName, type ThemePack } from '../types';
